import React from "react";
import "../styles/components/_bookmarks.scss";

export default function Bookmarks({ userId }) {
    return (
        <div className="bookmarks">
            {/* TODO: fetch and list bookmarked posts */}
            <p>Bookmarks component placeholder.</p>
        </div>
    );
}
